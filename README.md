# llm-qualitative


## Instructions to run

#### Pre-requisites
- Docker installed and running

#### Steps
1. Clone the repository
2. With the repository root opened on a terminal window run: `docker-compose up --build`
3. You should now see 3 containers running on your docker desktop app or other viewer.
4. Head to `http://localhost:5173` to view the app!
